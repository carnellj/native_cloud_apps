spring:
  application:
    name: organizationservice
  profiles:
    active:
      default
  cloud:
    config:
      enabled: true
      discovery:
        enabled: true
        serviceId: configserver
